@using Microsoft.AspNetCore.Mvc.TagHelpers
@model CitizenPanel.UI.MVC.Models.QuestionnaireModules.EditQuestionnaireViewModel

@{
    ViewBag.Title = "Bewerk vragenlijst";
    bool isDiscover = Model.Id == 1;
}
@section Styles {
    <link rel="stylesheet" href="~/dist/clientapp/assets/styles.css" asp-append-version="true"/>
    <link rel="stylesheet" href="@Url.Content("https://cdn.jsdelivr.net/npm/bootstrap-icons/font/bootstrap-icons.css")">
}


<div class="recruitment-criteria-page-container">
    <div class="recruitment-criteria-header">
        <h2>Bewerk @Model.Title</h2>
        <button type="button" class="btn btn-outline-primary btn-sm-equivalent" id="toggle-all-btn">
            Alles invouwen
        </button>
    </div>

    <form asp-action="EditQuestionnaire" method="post">
        <input type="hidden" id="questionnaire-id" asp-for="Id" value="@Model.Id"/>
        <input type="hidden" asp-for="Title" value="@Model.Title"/>
        <span asp-validation-for="@Model.Questions" class="text-danger small"></span>

        <ul id="questions-list" class="criteria-list">
            @for (int i = 0; i < Model.Questions.Count; i++)
            {
                <li class="criteria-item-card">
                    <input asp-for="@Model.Questions[@i].Id" type="hidden" value="@Model.Questions[@i].Id" class="question-id"/>
                    <input asp-for="@Model.Questions[@i].ToDelete" type="hidden" class="question-delete"/>
                    <div class="criteria-card-header">
                        <div class="fs-5 me-3">
                            <span class="drag-handle">&#x2630;</span>
                        </div>

                        <div class="criteria-header-content">
                            <label class="criteria-list-label question-number">Vraag @(i + 1)</label>
                            <div class="criteria-header-input-group">
                                <div class="flex-grow-1">
                                    <input asp-for="@Model.Questions[@i].Description" class="auth-input-field criteria-description"/>
                                    <span asp-validation-for="@Model.Questions[@i].Description" class="text-danger small"></span>
                                </div>
                                <button type="button" class="btn btn-danger btn-sm-equivalent btn-remove-criteria">
                                    <i class="fas fa-trash"></i>
                                </button>
                                @if (!isDiscover)
                                {
                                    <div class="ml-4">
                                        <input type="hidden" class="detail-value"/>
                                        <input asp-for="@Model.Questions[@i].IsDetail"
                                               class="form-check-input detail-check" type="checkbox"/>
                                        <label class="panel-form-label pr-1">Detail?</label>
                                    </div>
                                }
                            </div>
                        </div>
                        <button type="button" class="criteria-expand-button"
                                aria-expanded="true"
                                aria-controls="question-body-@i">
                            <i class="fas fa-chevron-up"></i>
                        </button>
                    </div>
                    <div class="criteria-card-body-wrapper" id="question-body-@i">
                        <div class="criteria-card-body card-body">
                            <label class="subcriteria-list-label">Antwoorden</label>
                            <span asp-validation-for="@Model.Questions[@i].Answers" class="text-danger subcriteria-warning"></span>
                            <ul id="answers-list-@i" class="subcriteria-list">
                                @for (int j = 0; j < Model.Questions[i].Answers.Count; j++)
                                {
                                    <li class="subcriteria-item-row">
                                        <input asp-for="@Model.Questions[@i].Answers[@j].Id" type="hidden" class="answer-id"
                                               value="@Model.Questions[@i].Answers[@j].Id"/>
                                        <input asp-for="@Model.Questions[@i].Answers[@j].ToDelete" type="hidden"
                                               class="answer-delete"/>
                                        <div class="col-auto fs-5 p-0">
                                            <span class="drag-handle">&#x2630;</span>
                                        </div>

                                        <div class="col">
                                            <div class="subcriteria-input-name-wrapper">
                                                <input asp-for="@Model.Questions[@i].Answers[@j].Description"
                                                       id="Description-@i-@j"
                                                       class="auth-input-field subcriteria-description"
                                                       placeholder="Beschrijving"/>
                                                <label for="Description-@i-@j">Beschrijving</label>
                                            </div>
                                            <span asp-validation-for="@Model.Questions[@i].Answers[@j].Description"
                                                  class="auth-validation-message"></span>
                                                <div class="subcriteria-input-name-wrapper">
                                                    <input asp-for="@Model.Questions[@i].Answers[@j].Advice"
                                                           id="Advice-@i-@j"
                                                           class="auth-input-field answer-advice"
                                                           placeholder="Aanbeveling"/>
                                                    <label for="Advice-@i-@j" class="advice-label">Aanbeveling</label>
                                                </div>
                                            <span asp-validation-for="@Model.Questions[@i].Answers[@j].Advice"
                                                  class="auth-validation-message"></span>
                                        </div>

                                        <div class="subcriteria-remove-button-wrapper">
                                            <button type="button" class="btn btn-danger btn-sm-equivalent btn-remove-subcriteria">
                                                Verwijder
                                            </button>
                                        </div>
                                        @if (isDiscover)
                                        {
                                            <div class="col-auto form-check">
                                                <input type="hidden" value="@Model.Questions[@i].Answers[@j].IsCritical"
                                                       class="critical-value"/>
                                                <input asp-for="@Model.Questions[@i].Answers[@j].IsCritical"
                                                       class="form-check-input critical-check" type="checkbox"/>
                                                <label class="form-check-label">Breekpunt</label>
                                            </div>
                                        }
                                    </li>
                                }
                            </ul>
                            <div class="subcriteria-footer">
                                <button type="button" class="btn btn-secondary btn-sm-equivalent btn-add-subcriteria" question-index="@i">Voeg
                                    antwoordoptie toe
                                </button>
                            </div>
                        </div>
                    </div>
                </li>
            }
        </ul>

        <div class="recruitment-criteria-actions">
            <button type="button" class="btn btn-secondary" id="add-question-btn">Voeg vraag toe</button>
            <button type="submit" class="btn btn-primary">Opslaan</button>
        </div>
    </form></div>

@section Scripts {
    <script type="module" src="~/dist/clientapp/assets/admin.js"></script>
}